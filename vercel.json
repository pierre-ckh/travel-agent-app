```json
{
  "version": 2,
  
  "// Build Configuration": "Defines how Vercel builds and deploys the application",
  "builds": [
    {
      "// FastAPI Backend": "Configure FastAPI as a serverless function",
      "src": "main.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.10",
        "maxLambdaSize": "15mb"
      }
    },
    {
      "// Streamlit Frontend": "Configure Streamlit app deployment",
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.10",
        "maxLambdaSize": "15mb"
      }
    }
  ],
  
  "// Routing Configuration": "Define how requests are routed to different parts of the application",
  "routes": [
    {
      "// API Routes": "Route all /api/* requests to FastAPI backend",
      "src": "/api/(.*)",
      "dest": "main.py",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "headers": {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Methods": "GET, POST, PUT, DELETE, PATCH, OPTIONS",
        "Access-Control-Allow-Headers": "X-Requested-With, Content-Type, Authorization"
      }
    },
    {
      "// Static Assets": "Serve static files directly",
      "src": "/static/(.*)",
      "headers": {
        "Cache-Control": "public, max-age=31536000, immutable"
      }
    },
    {
      "// Frontend Routes": "Route all other requests to Streamlit frontend",
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  
  "// Environment Variables": "Define required environment variables for the application",
  "env": {
    "ANTHROPIC_API_KEY": "@anthropic_api_key",
    "AMADEUS_API_KEY": "@amadeus_api_key",
    "AMADEUS_API_SECRET": "@amadeus_api_secret",
    "BOOKING_API_KEY": "@booking_api_key",
    "MAILGUN_API_KEY": "@mailgun_api_key",
    "MAILGUN_DOMAIN": "@mailgun_domain",
    "DATABASE_URL": "@database_url"
  },
  
  "// Build Environment Variables": "Environment variables available during build time",
  "build": {
    "env": {
      "ANTHROPIC_API_KEY": "@anthropic_api_key",
      "AMADEUS_API_KEY": "@amadeus_api_key",
      "AMADEUS_API_SECRET": "@amadeus_api_secret",
      "BOOKING_API_KEY": "@booking_api_key",
      "MAILGUN_API_KEY": "@mailgun_api_key",
      "MAILGUN_DOMAIN": "@mailgun_domain",
      "DATABASE_URL": "@database_url"
    }
  },
  
  "// Function Configuration": "Configure serverless function settings",
  "functions": {
    "main.py": {
      "runtime": "python3.10",
      "memory": 1024,
      "maxDuration": 30,
      "environment": {
        "ANTHROPIC_API_KEY": "@anthropic_api_key",
        "AMADEUS_API_KEY": "@amadeus_api_key",
        "AMADEUS_API_SECRET": "@amadeus_api_secret",
        "BOOKING_API_KEY": "@booking_api_key",
        "MAILGUN_API_KEY": "@mailgun_api_key",
        "MAILGUN_DOMAIN": "@mailgun_domain",
        "DATABASE_URL": "@database_url"
      }
    },
    "app.py": {
      "runtime": "python3.10",
      "memory": 1024,
      "maxDuration": 30,
      "environment": {
        "ANTHROPIC_API_KEY": "@anthropic_api_key",
        "AMADEUS_API_KEY": "@amadeus_api_key",
        "AMADEUS_API_SECRET": "@amadeus_api_secret",
        "BOOKING_API_KEY": "@booking_api_key",
        "MAILGUN_API_KEY": "@mailgun_api_key",
        "MAILGUN_DOMAIN": "@mailgun_domain",
        "DATABASE_URL": "@database_url"
      }
    }
  },
  
  "// Headers Configuration": "Global headers for security and optimization",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "SAMEORIGIN"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ],
  
  "// Redirects": "Handle common redirect scenarios",
  "redirects": [
    {
      "source": "/api",
      "destination": "/api/",
      "permanent": true
    }
  ],
  
  "// Python Configuration": "Additional Python-specific settings",
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "echo 'Build complete'",
  "outputDirectory": ".",
  
  "// Framework Detection": "Disable automatic framework detection",
  "framework": null,
  
  "// Regional Configuration": "Deploy to specific regions for better performance",
  "regions": ["iad1"]
}
```

Additionally, create these supplementary files for proper deployment:

**requirements.txt** (example):
```txt
fastapi==0.104.1
streamlit==1.28.2
uvicorn==0.24.0
python-multipart==0.0.6
anthropic==0.7.1
amadeus==9.0.0
requests==2.31.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
pydantic==2.5.0
python-dotenv==1.0.0
```

**api/index.py** (wrapper for FastAPI):
```python
"""
Vercel serverless function wrapper for FastAPI app
"""
from main import app

# Export the FastAPI app for Vercel
handler = app
```

**streamlit_config.toml** (.streamlit/config.toml):
```toml
[server]
headless = true
port = 8501
enableCORS = false
enableXsrfProtection = false

[browser]
serverAddress = "0.0.0.0"
serverPort = 8501
```

**Note**: 
- Environment variables prefixed with `@` (e.g., `@anthropic_api_key`) reference Vercel secrets that should be set in your Vercel dashboard
- Adjust memory and maxDuration based on your application's needs
- The region can be changed based on your target audience location